# 视频即指令流：一种替代编码分发的实时重绘架构构想

**Video as a Command Stream — A Real-Time Re-Render Architecture to Replace Codec-Based Video Delivery**

> 公开占位声明 / Concept timestamp
> 本构想形成于约 2010 年代初（3D 游戏实时渲染兴起的年代），由作者独立提出，远早于神经渲染、生成式视频与云游戏串流成为行业热点的时间。本文档作为该构想的公开记录与时间戳，意在占据这一架构概念位。

## 0. 一句话主张

视频不应被当作"压缩后传输的像素流"，而应被当作"传送到接收端、用于实时重绘的指令流"。

一旦完成这层范式转换，"视频"的定义就从**传输的信号**变成**生成的指令**。

## 1. 背景与缘起

作者为视频编码一线实践者：

- 2013 年前后，曾在 TLF 发布过早期 HEVC（H.265）压制作品；
- 近年亦已实践最新一代 VVC（H.266）编码；
- 在 3D 游戏实时渲染刚刚兴起的年代（约 2010 年前后），便产生一个反直觉的直觉：

> 既然游戏能用"传状态 + 本地实时渲染"呈现逼真画面，视频为什么必须是"编码像素 + 解码播放"？

当时 HEVC 尚未定稿、神经网络尚未成为热点、云概念尚未普及。在几乎没有任何现实零件支撑的年代，这一判断纯靠第一性原理得出。

## 2. 核心架构（pipeline）

```
[原始视频 footage]
   │
   ▼
[AI 视觉模型：抽取语义元数据]
   │
   ▼
[AI 专家模型：在云端素材库中匹配对应场景]
   │
   ▼
[抽象为"调取命令"（极小体积）]
   │
   ▼
[网络只传输命令，不传输像素]
   │
   ▼
[客户端用实时渲染引擎（如虚幻引擎）重绘]
   │
   ▼
[完成]
```

关键点：**网上跑的不再是视频本身，而是一条"用哪个资产、怎么摆、什么光照"的紧凑指令。**

## 3. 关键洞察：编码与生成同构（"相都一样"）

传统视频编码的本质，早就是一套"低抽象度的生成"：

- DCT / 变换基 = 一套固定的、通用的纹理基元库（余弦波形）；
- 运动矢量、帧间预测 = "别重传了，参照前一帧那块素材，挪一下位置"；
- 解码器 = 那个"引擎"，拿着系数（参数）去基元库里查、加权、重建画面。

所以 H.264 干的事，抽象地看，**与"实时重绘"是同一件事**：传参数、本地查库、引擎重绘。区别只在素材库的抽象度：

| 层级 | 素材库类型 | 例子 |
|------|-----------|------|
| 低（编码） | 信号级基元 | 正弦波、宏块、残差 |
| 高（本构想） | 语义级 / 对象级资产 | 一棵树、一张脸、一段光照 |

**抽象度决定压缩比上限。** 素材库越接近世界的真实结构，单位信息能表达的内容越多。编码卡在信号层，受香农墙约束；把库提到语义 / 对象层，墙就换了一堵。

结论：编码（路径 1）与生成重绘（路径 2）**内核同构**，不是两个赛道。"编码会不会被取代"不是问题——它会被"抽象度升级"自然吞并。

## 4. 素材库不是"建"出来的，是"长"出来的

素材库 = AI 对所有视频做元抽象之后，持续沉淀、自我生长的产物。不是运行一次生成的静态库，也不是每段视频现搓一份。

- 新 footage 进来 → 视觉模型抽元数据 → 专家模型去**已存在、持续生长**的库中匹配 → 抽象成调取命令；
- 库随处理的视频变多而越全，覆盖自增长；
- 抽象能力越强，库越向有限的基础基元收敛（尺寸收敛 / plateau，而非无限膨胀）——因为"这棵树的 100 种变体"会归并成"树 + 参数"一条。

这意味着带宽赢面是**结构上保证**的：footage 映射到的是库里已有的抽象元素，天然紧凑、可跨内容复用。

## 5. 它是一个自强化循环，不是分散模块

带宽、覆盖、存储、算力不是四个独立问题，是同一条积累曲线的切面：

- 库吃得越多 → 覆盖越广 → 指令越短 → 带宽越省 → 护城河越深。

它们互相长，不互相拖。库自身的存储膨胀，会被抽象能力的提升自我收敛——这是同一个主驱动力在不同切面的表现。

## 6. 为什么现在技术已接近可行

所需的零件，今天全部已就位（只是分散）：

- **视觉抽象**：视频理解大模型已就绪；
- **素材库**：世界模型 / 生成模型的隐空间即其雏形；
- **实时重绘**：虚幻引擎 5（Nanite + Lumen + Metahuman）对大量内容已够真；
- **串流分发**：云游戏架构已被验证数十亿次。

零件齐了，缺的只是"把它们焊成一个目标"的整合力，而非技术突破。

## 7. 行业对照：现有玩家在哪个抽象度

| 玩家 | 做法 | 抽象度层级 |
|------|------|-----------|
| 传统编码 H.264 / HEVC / VVC | 信号级压缩 | 最低 |
| 神经编码（腾讯 TNC、临科智华） | AI 增强潜变量压缩 | 略高，仍在压缩范式内 |
| WaveOne（苹果收购） | 内容感知压缩，"理解帧"、优先人脸 | 压缩范式内的高一层，但仍是传压缩数据 |
| MPEG V3C | 传几何 + 纹理，接收端渲染 | 接近本构想，但限于体积 / 3D 捕获 |
| **本构想** | footage → 语义指令 → 云端库匹配 → 客户端实时重绘 | **语义级可编辑指令，通用 footage** |

最像本构想的 WaveOne，也被苹果收了，但其抽象度仍比"传指令 + 客户端重绘"低一档——它传的是压缩数据，不是可编辑的语义指令。

## 8. 盈利逻辑：成本效率反超即盈利

- 编码分发成本随观看量**线性涨**（每多一个观众多一份带宽）；
- 本架构成本随库成熟**下降**（看得越多，库越全，指令越短，单位成本越低）；
- 一旦"传指令 + 重绘"的单位成本低于"编码 + 投送"，省下的带宽即为纯毛利，且优势自我强化。

视频占互联网流量主体，带宽每天在烧真金。谁先把这套跑通，谁吃的是结构性的、持续性的带宽市场。

## 9. 为什么"没人做"（整合位空着）

- **巨头是组合者、概念滞后**：各自推自己那块 KPI（AV1 / 渲染 / 生成），没人站在"这几样合起来是视频的下一个范式"的认知高度去整合；
- **做生成的人没往"取代 codec"想，做 codec 的人困在信号层出不来**——中间这条缝，真空着；
- 概念原创性 × 执行力 = 领先。目前"想到的人"没同时握齐"编码老底子 + 生成新认知 + 去弄的资源"。

## 10. 结语

世界的意义，也许就是从无数个空想开始。本文件作为该构想的公开占位与时间戳记录——概念先于零件，思想实验有其意义。

---

*作者：视频编码老手艺人（早年曾在 TLF 发布过 HEVC 压制作品；近年亦实践 VVC）。本构想早于行业热点，独立提出，远约十余年。*
